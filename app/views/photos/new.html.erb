<h1>New photo</h1>

<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul>
  <% tags = Tag.all%>
  <% tags.each{|tag| %>
      <li><input type="checkbox" name="[]" class="exist-list" value="<%= tag.name %>"　name="tags[]"> <%= tag.name %></input></li>
  <% } %>
  </ul>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', photos_path %>

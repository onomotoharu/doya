<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h1>signup page</h1>
  <!-- signup -->
  <div class="field" >
    <%= f.label :name %><br>
    <%= f.text_field :name  ,:id=>"name-field-id" , :placeholder=>"username" %>
  </div>
  <div class="field">
    <%= f.label :password_digest %><br>
    <%= f.text_field :password_digest ,:placeholder=>"password" ,:id=>"signup-password" ,:type=>"password"%>
  </div>
  <div class="field">
    <%= f.label :password_digest %><br>
    <%= f.text_field :password_digest ,:placeholder=>"confirm-password" ,:id=>"signup-confirm-password",:type=>"password"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
